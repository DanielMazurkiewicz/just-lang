export const functionExample1 = `
    functionName () {}
`

export const functionExample2 = `
    functionName (arg1 [Type = U8 | U32]; arg2 [Type]; kind ENUM_TYPE) {
        x Type = arg1 / 2;
        y Type = arg2 / 2;
        => x + y;
    }
`
